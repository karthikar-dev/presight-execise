{
  "name": "presight-execise",
  "version": "1.0.0",
  "repository": "",
  "author": "",
  "license": "MIT",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "start": "lerna run start --parallel",
    "start:server": "lerna run start --scope=presight-server",
    "start:client": "lerna run start --scope=presight-client"
  },
  "devDependencies": {
    "lerna": "^9.0.1"
  }
}
